{
  "name": "agentpulse-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute agentpulse --file=schema.sql",
    "db:init:local": "wrangler d1 execute agentpulse --local --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
